<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.mmutert.trackmydebt.ui.persondetail.PersonDetailViewModel" />

        <variable
            name="formattedSum"
            type="String" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/header_layout"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:background="@color/design_default_color_primary"
                android:padding="16dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:drawableEnd="@drawable/ic_brand_paypal_24_black"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:id="@+id/bt_paypal"
                    android:scaleType="centerInside" />

                <TextView
                    android:id="@+id/tv_debt_description"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintTop_toTopOf="@+id/tv_sum"
                    app:layout_constraintStart_toStartOf="parent"
                    android:textSize="20sp"
                    android:textColor="@android:color/primary_text_dark"
                    tools:text="Debt:"
                    app:layout_constraintBottom_toBottomOf="@+id/tv_sum" />

                <TextView
                    android:id="@+id/tv_sum"
                    android:text='@{formattedSum}'
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    app:layout_constraintTop_toTopOf="@+id/bt_paypal"
                    app:layout_constraintStart_toEndOf="@id/tv_debt_description"
                    android:textSize="20sp"
                    android:textColor="@android:color/primary_text_dark"
                    tools:text="5.00€"
                    app:layout_constraintBottom_toBottomOf="@+id/bt_paypal" />
            </androidx.constraintlayout.widget.ConstraintLayout>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/list"
                app:layout_constraintTop_toBottomOf="@id/header_layout"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:name="com.mmutert.trackmydebt.ui.persondetail.PersonDetailFragment"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layoutManager="LinearLayoutManager"
                tools:context=".ui.persondetail.PersonDetailFragment"
                tools:listitem="@layout/item_person_detail" />
        </LinearLayout>

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="true"
            android:layout_gravity="end|bottom"
            android:layout_margin="16dp"
            android:text="Transaction"
            app:icon="@android:drawable/ic_input_add"
            android:id="@+id/fab_add_transaction"
            android:contentDescription="Add transaction button" />


    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>
